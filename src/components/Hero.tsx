import React from "react";
import Navigation from "./Navigation";
import HeaderTitle from "./HeaderTitle";

const Hero = () => {
  return (
    <div className="hero">
      <Navigation />
      <HeaderTitle />
    </div>
  );
};

export default Hero;
